# Use the modern built-in Keycloak v2 theme as the compatibility base for the current Keycloak release.
# This keeps the native login layout and assets available while the custom Keywind layer overrides the UI.
parent=keycloak.v2

styles=dist/index.css
scripts=dist/index.js

locales=vi,en